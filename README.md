# Bavaria Motors email template

Simple html email template for Bavaria motors emails.  For each new campaign, create a new branch off the last branch and label it `blastXX`.  Host all images on MailChimp.
